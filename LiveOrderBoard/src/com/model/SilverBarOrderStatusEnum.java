package com.model;

/**
 * Order Status Enum
 * Currently supports only LIVE and CANCELLED
 * @author panigs
 *
 */

public enum SilverBarOrderStatusEnum {
	LIVE, CANCELLED;
	
	// Kept it simple, we could have arguments, constructors and valueOf implemented.
}
